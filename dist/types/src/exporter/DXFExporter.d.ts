export class DXFExporter {
    static measurementPointSection(measurement: any): string;
    static measurementPolylineSection(measurement: any): string;
    static measurementSection(measurement: any): string;
    static toString(measurements: any): string;
}
